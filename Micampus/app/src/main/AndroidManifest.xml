<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="mx.edu.cetys.garay.andrea.micampus">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".view.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".view.MainMenuActivity"
            android:label="Main Menu" />

        <activity android:name=".view.PerfilListViewActivity" />

        <activity
            android:name=".view.PerfilActivity"
            android:label="Perfil" />
        <activity
            android:name=".view.BoletaListViewActivity"
            android:label="Boleta"
            android:parentActivityName=".view.MainMenuActivity"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".view.TutoresViewActivity"
            android:label="Información" />
    </application>

</manifest>